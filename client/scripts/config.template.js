const config = {
    MAPBOX_TOKEN: 'votre token Mapbox',
    STUN_SERVERS: {
        iceServers: [
            {
                urls: ['stun:stun.l.google.com:19302']
            }
        ]
    }
};